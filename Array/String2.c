#include<stdio.h>
int main(){

    char str[20];
    int i;
    printf("enter the string");
    scanf("%s",str);

    for(i=0;str[i]!='\0';i++){

    }
    printf("the length of the string is: %d",i);

}